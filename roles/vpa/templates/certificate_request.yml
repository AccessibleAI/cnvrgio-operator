apiVersion: certificates.k8s.io/v1beta1
kind: CertificateSigningRequest
metadata:
  name: vpa-tls-certs
  namespace: "{{ansible_operator_meta.namespace}}"
spec:
  request: {{ server_request.stdout }}
  usages:
  - digital signature
  - key encipherment
  - server auth