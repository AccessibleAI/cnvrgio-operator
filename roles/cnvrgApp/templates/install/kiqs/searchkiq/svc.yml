apiVersion: v1
kind: Service
metadata:
  name: searchkiq
  namespace: {{ ansible_operator_meta.namespace }}
  labels:
    app: searchkiq
    exporter: ruby-exporter
spec:
  ports:
    - name: metrics
      port: 9394
  selector:
    app: searchkiq
