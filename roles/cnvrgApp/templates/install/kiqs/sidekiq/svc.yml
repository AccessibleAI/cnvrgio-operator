apiVersion: v1
kind: Service
metadata:
  name: sidekiq
  namespace: {{ ansible_operator_meta.namespace }}
  labels:
    app: sidekiq
    exporter: ruby-exporter
spec:
  ports:
    - name: metrics
      port: 9394
  selector:
    app: sidekiq
