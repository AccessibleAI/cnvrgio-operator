---
ingress:
  enabled: "true" #
  timeout: 900s #  15min = retries_attempts * per_try_timeout
  retries_attempts: 5
  per_try_timeout: 180s
  descriptions:
    ingress:
      enabled: |
        either create ingress rules or not based on `ingressType`,
        if set to false,
        none of the ingress rules will be created







