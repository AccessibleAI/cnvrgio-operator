#jinja2: trim_blocks: "true", lstrip_blocks: "false"
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: "{{istio.gwName}}"
  namespace: {{ ansible_operator_meta.namespace }}
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "{{('*.' + clusterDomain).split(':')[0] }}"
    {% if https.enabled == "true" and https.certSecret != "" %}
      tls:
        httpsRedirect: true
    - hosts:
        - "{{('*.' + clusterDomain).split(':')[0] }}"
      port:
        name: https
        number: 443
        protocol: HTTPS
      tls:
        mode: SIMPLE
        credentialName: "{{ https.certSecret }}"
    {% elif https.enabled == "true" %}
      tls:
        httpsRedirect: true
    - hosts:
        - "{{('*.' + clusterDomain).split(':')[0] }}"
      port:
        name: https
        number: 443
        protocol: HTTP
    {% endif %}
