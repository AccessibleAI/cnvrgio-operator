apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{cnvrgRouter.svcName}}
  namespace: {{ ansible_operator_meta.namespace }}
  labels:
    app: {{cnvrgRouter.svcName}}
spec:
  host: "{{(cnvrgRouter.svcName + '.' + clusterDomain).split(':')[0] }}"
  port:
    targetPort: {{cnvrgRouter.port}}
  to:
    kind: Service
    name: {{cnvrgRouter.svcName}}
    weight: 100
{% if https.enabled == "true" %}
  tls:
    termination: edge
    certificate: |
      {{https.cert | b64decode | indent(6) }}
    key: |
      {{https.key | b64decode| indent(6) }}
    insecureEdgeTerminationPolicy: Redirect
{% endif %}