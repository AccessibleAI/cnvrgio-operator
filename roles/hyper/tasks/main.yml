# Install  Hyper
- name: Install Hyper Server
  include_role:
    name: common
    tasks_from: install
  vars:
    templatePath: "{{ item }}"
    state: "{{ roleState }}"
  with_fileglob:
    - templates/*