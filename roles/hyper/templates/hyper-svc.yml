apiVersion: v1
kind: Service
metadata:
  name: "{{ hyper.svcName }}"
  namespace: "{{ ansible_operator_meta.namespace }}"
  labels:
    app: "{{ hyper.svcName }}"
spec:
{% if ingress_type == "nodeport" %}
  type: NodePort
{% endif %}
  ports:
    - port: {{hyper.port}}
{% if ingress_type == "nodeport" %}
      nodePort: {{ hyper.nodePort }}
{% endif %}
  selector:
    app: {{hyper.svcName}}