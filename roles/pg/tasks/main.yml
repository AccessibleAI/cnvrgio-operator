# Install  PG
- name: Install Postgres
  include_role:
    name: common
    tasks_from: install
  vars:
    templatePath: "{{ item }}"
    state: "present"
  with_items:
    - templates/pg-pvc.yml
    - templates/pg-svc.yml
    - templates/pg-dep.yml