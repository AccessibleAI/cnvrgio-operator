---
- name: Install Kibana
  include_role:
    name: common
    tasks_from: install
  vars:
    templatePath: "{{ item }}"
    state: "present"
  with_items:
    - templates/kibana/dep.yml
    - templates/kibana/svc.yml
