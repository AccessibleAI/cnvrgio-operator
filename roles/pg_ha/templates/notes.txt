downside to customresource
1. need to create threee secrets for superuser standard user and replication user
2. need to create ssh keys for pgbackrest

creating pgcluster with pclient is easier 
1. it creates everything with it, and the ability to change configuration like superuser pass
1a. 1. pgo create cluster postgres --password-superuser=pg_pass




common commands
export PGO_NAMESPACE=cnvrg
pgo show user --show-system-accounts --all
pgo create cluster postgres --password-superuser=$POSTGRES_PASSWORD --username=$POSTGRES_USER --password=$POSTGRES_PASSWORD \
--custom-config=postgres-custom-config --cpu= --memory= 
--node-label which node to put primate db