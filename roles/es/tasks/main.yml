# Install  PG
- name: Install ElasticSearch
  include_role:
    name: common
    tasks_from: install
  vars:
    template_path: "{{ item }}"
    state: "{{ role_state }}"
  with_items:
    - templates/es/svc.yml
    - templates/es/sts.yml
